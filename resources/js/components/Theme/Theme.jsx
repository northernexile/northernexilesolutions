import { createTheme } from "@mui/material/styles";

export const appTheme = createTheme({
    palette: {
        type: 'light',
        primary: {
            main: '#0a5425'
        },
        secondary: {
            main: '#da1b07'
        },
    },
});
